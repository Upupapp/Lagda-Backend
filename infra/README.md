# infra

Deployment and operational support for the LAGDA backend.

**Status:** empty at the foundation stage.

Will eventually hold deployment support, migration execution, and operational
configuration. It must not contain application business code — that belongs in
`packages/`.
