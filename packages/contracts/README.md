# @lagda/contracts

**Status:** foundation only. No implementation yet — see `../../README.md`.

## Responsibility

Shared API and domain boundary contracts: branded IDs, runtime request/response schemas, enums, discriminated unions, error shapes, pagination contracts, public DTOs.

## Must not contain

Database entities, ORM models, Fastify request objects, browser-only utilities, frontend framework code, or any infrastructure dependency.

## Compile-time dependencies

none
