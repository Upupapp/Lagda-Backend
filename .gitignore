node_modules/
dist/
coverage/
*.tsbuildinfo

# Secrets. `.env.example` is deliberately allowed through — it is the documented
# template and contains no real values.
.env
.env.*
!.env.example

# Local-only dev Postgres (not used for deployment)
docker-compose.local.yml
scripts/local/

# Claude Code local settings — personal, and carries a production host
# address. Never committed.
.claude/settings.local.json
